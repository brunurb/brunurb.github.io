name: Mosaica

on:
  push:
    paths:
      - 'mosaica/pictures/**'
      - 'mosaica/index.html'
  workflow_dispatch:

jobs:
  update-gallery:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Generate index.html
        run: |
          echo "Generating index.html..."
          echo '<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">' > mosaica/index.html
          echo '<title>Mosaic Gallery</title>' >> mosaica/index.html
          echo '<link href="https://fonts.googleapis.com/css?family=Press+Start+2P" rel="stylesheet">' >> mosaica/index.html
          echo '<link href="https://unpkg.com/nes.css/css/nes.css" rel="stylesheet">' >> mosaica/index.html
          echo '<link rel="stylesheet" type="text/css" href="jquery.mosaic.css" />' >> mosaica/index.html
          echo '<link rel="stylesheet" type="text/css" href="styles.css" />' >> mosaica/index.html
          echo '<style>#myMosaic { margin: 0; padding: 0; display: flex; flex-wrap: wrap; justify-content: center; } img { margin: 0; padding: 0; display: block; border: none;}</style>' >> mosaica/index.html
          echo '<style>#myMosaic html, body, pre, code, kbd, samp {
          font-family: "Press+Start+2P&display=swap";}</style>' >> mosaica/index.html
          echo '</head><body>' >> mosaica/index.html

          # Here is the menu code
          echo '<div class="nes-btn" id="myTopnav">' >> mosaica/index.html
          echo '  <a href="https://brunurb.github.io/">Home</a>' >> mosaica/index.html
          echo '  <button type="button" class="nes-btn is-warning">917</button><a href="#917">917</a>' >> mosaica/index.html
          echo '  <a href="#GUI" class="active">GUI</a>' >> mosaica/index.html
          echo '  <a href="#news">News</a>' >> mosaica/index.html
          echo '  <a href="#contact">Contact</a>' >> mosaica/index.html
          echo '  <a href="#about">About</a>' >> mosaica/index.html
          echo '  <a href="javascript:void(0);" class="icon" onclick="myFunction()">' >> mosaica/index.html
          echo '    <i class="fa fa-bars"></i>' >> mosaica/index.html
          echo '  </a>' >> mosaica/index.html
          echo '</div>' >> mosaica/index.html
          echo '<script>function myFunction() { var x = document.getElementById("myTopnav"); if (x.className === "topnav") { x.className += " responsive"; } else { x.className = "topnav"; } }</script>' >> mosaica/index.html

          # Mosaic Image Generation
          echo '<div id="myMosaic">' >> mosaica/index.html
          for img in $(ls mosaica/pictures | grep -E '\.jpeg$|\.jpg$|\.gif$|\.svg$|\.png$|\.webp$'); do
            echo "<img src='pictures/$img' alt='Gallery Image' />" >> mosaica/index.html
          done

          echo '</div>' >> mosaica/index.html
          echo '<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>' >> mosaica/index.html
          echo '<script type="text/javascript" src="jquery.mosaic.js"></script>' >> mosaica/index.html
          echo '<script type="text/javascript" src="scripts.js"></script>' >> mosaica/index.html
          echo '<script>$(document).ready(function() { $("#myMosaic").Mosaic(); });</script>' >> mosaica/index.html
          echo '</body></html>' >> mosaica/index.html

      - name: Commit changes
        run: |
          git config --local user.email "github-actions@github.com"
          git config --local user.name "GitHub Action"
          git add mosaica/index.html
          git commit -m "Regenerate index.html for mosaic gallery" || echo "No changes to commit"
          git push origin master  # Make sure this matches your main branch
