name: Planets

on:
  push:
    paths:
      - 'majortom/planets/pictures/**'
      - 'majortom/planets.html'
  workflow_dispatch:

jobs:
  update-gallery:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Generate planets.html
        run: |
          echo "Generating planets.html..."
          echo '<!DOCTYPE html><html lang="en">' > majortom/planets.html
          echo '<head>' >> majortom/planets.html
          echo '<meta charset="UTF-8">' >> majortom/planets.html
          echo '<meta http-equiv="X-UA-Compatible" content="IE=edge">' >> majortom/planets.html
          echo '<meta name="viewport" content="width=device-width, initial-scale=1.0">' >> majortom/planets.html
          echo '<title>Planets Gallery</title>' >> majortom/planets.html
          echo '<link rel="stylesheet" href="https://unpkg.com/nes.css/css/nes.css" />' >> majortom/planets.html
          echo '<style>' >> majortom/planets.html
          # Add your CSS here; you could include specific styles for planets.html
          echo '</style>' >> majortom/planets.html
          echo '</head><body>' >> majortom/planets.html

          # Header
          echo '<div class="header">' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/"><span class="nes-icon home is-small"></span> brunurb</a>' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/majortom/917.html" class="nes-btn">917</a>' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/majortom/astronaut.html" class="nes-btn">Astronauts</a>' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/majortom/controls.html" class="nes-btn">Controls</a>' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/majortom/spaceships.html" class="nes-btn">Spaceships</a>' >> majortom/planets.html
          echo '</div>' >> majortom/planets.html

          # Main content with centered text in mosaic div
          echo '<main>' >> majortom/planets.html
          echo '<h1>Planets Gallery</h1>' >> majortom/planets.html
          echo '<div id="myMosaic">' >> majortom/planets.html

          # Loop to add images inside the myMosaic div
          for img in $(ls majortom/planets/pictures | grep -E '\.jpeg$|\.jpg$|\.gif$|\.svg$|\.png$|\.webp$'); do
            echo "<img src='https://brunurb.github.io/majortom/planets/pictures/$img' alt='Gallery Image' data-full='https://brunurb.github.io/majortom/planets/pictures/$img' class='thumbnail' onclick='showImage(this.dataset.full)' />" >> majortom/planets.html
          done

          echo '</div>' >> majortom/planets.html
          echo '<div id="overlay" onclick="closeOverlay(event)">' >> majortom/planets.html
          echo '  <div class="overlay-content">' >> majortom/planets.html
          echo '    <span id="close">&times;</span>' >> majortom/planets.html
          echo '    <img id="overlay-image" src="" alt="Full Size Image" />' >> majortom/planets.html
          echo '  </div>' >> majortom/planets.html
          echo '</div>' >> majortom/planets.html
          echo '</main>' >> majortom/planets.html

          # Footer
          echo '<footer>© <a href="https://brunurb.github.io/">by brunurb</a></footer>' >> majortom/planets.html

          # Include jQuery and custom scripts
          echo '<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js"></script>' >> majortom/planets.html
          echo '<script type="text/javascript">' >> majortom/planets.html
          # Add the Overlay JS functionality here
          echo '</script>' >> majortom/planets.html
          echo '</body></html>' >> majortom/planets.html

      - name: Commit changes
        run: |
          git config --local user.email "github-actions@github.com"
          git config --local user.name "GitHub Action"
          git add majortom/planets.html
          git commit -m "Regenerate majortom/planets.html for gallery" || echo "No changes to commit"
          git push origin master  # Adjust this if your main branch is something else
