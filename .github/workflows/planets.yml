name: planets

on:
  push:
    paths:
      - 'majortom/planets/pictures/**'
      - 'majortom/planets.html'
  workflow_dispatch:

jobs:
  update-gallery:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Generate planets.html
        run: |
          echo "Generating planets.html..."
          echo '<!DOCTYPE html><html lang="en">' > majortom/planets.html
          echo '<head>' >> majortom/planets.html
          echo '<meta charset="UTF-8">' >> majortom/planets.html
          echo '<meta http-equiv="X-UA-Compatible" content="IE=edge">' >> majortom/planets.html
          echo '<meta name="viewport" content="width=device-width, initial-scale=1.0">' >> majortom/planets.html
          echo '<title>Intergalactic planetss</title>' >> majortom/planets.html
          echo '<link rel="preconnect" href="https://fonts.googleapis.com">' >> majortom/planets.html
          echo '<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>' >> majortom/planets.html
          echo '<link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">' >> majortom/planets.html
          echo '<link rel="stylesheet" href="https://unpkg.com/nes.css/css/nes.css" />' >> majortom/planets.html
          echo '<style>' >> majortom/planets.html
          echo 'body { background-color: #142936; color: #92cfbf; margin: 0; font-family: "Press Start 2P", cursive; }' >> majortom/planets.html
          echo '.header { position: fixed; top: 0; left: 0; right: 0; background-color: #224357; padding: 5px; display: flex; justify-content: space-between; align-items: center; box-shadow: 0px 4px 10px rgba(8, 17, 23, 0.9); z-index: 10; }' >> majortom/planets.html
          echo '.header a { color: #cd3162; text-decoration: none; font-family: "Press Start 2P", cursive; font-size: 1em; }' >> majortom/planets.html
          echo '.buttons { display: flex; gap: 2px; }' >> majortom/planets.html
          echo '#myMosaic { margin: 0; padding: 0; display: flex; flex-wrap: wrap; justify-content: center; margin-top: 20px; }' >> majortom/planets.html
          echo '#myMosaic img { border: none; max-width: calc(100% / 8); height: auto; cursor: pointer; }' >> majortom/planets.html
          echo 'h1 { text-align: center; color: #dedcff; margin: 80px 0 14px; font-size: 14px; }' >> majortom/planets.html
          echo 'footer { text-align: left; padding: 10px; font-size: 10px; background-color: #224357; color: #92cfbf; }' >> majortom/planets.html
          echo 'footer a { color: inherit; text-decoration: none; }' >> majortom/planets.html
          echo '#overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background-color: rgba(0, 0, 0, 0.8); display: none; z-index: 1000; justify-content: center; align-items: center; }' >> majortom/planets.html
          echo '#overlay.show { display: flex; }' >> majortom/planets.html
          echo '.overlay-content { display: flex; justify-content: center; align-items: center; max-width: 90%; max-height: 90%; }' >> majortom/planets.html
          echo '#overlay img { max-width: 100%; max-height: 100%; display: block; }' >> majortom/planets.html
          echo '#close { position: absolute; top: 20px; right: 30px; font-size: 40px; color: white; cursor: pointer; }' >> majortom/planets.html
          echo '</style>' >> majortom/planets.html
          echo '</head><body>' >> majortom/planets.html

          # Header
          echo '<div class="header">' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/"><span class="nes-icon home is-small"></span> brunurb</a>' >> majortom/planets.html
          echo '  <div class="buttons">' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/majortom/astronaut"><button type="button" class="nes-btn is-primary">Astronaut</button></a>' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/majortom/spaceships"><button type="button" class="nes-btn is-success">spacecraft</button></a>' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/majortom/controls"><button type="button" class="nes-btn is-warning">Controls</button></a>' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/majortom/planets"><button type="button" class="nes-btn is-error">Planets</button></a>' >> majortom/planets.html
          echo '  <a href="https://brunurb.github.io/majortom/917"><button type="button" class="nes-btn is-warning">917</button></a>' >> majortom/planets.html
          echo '  </div>' >> majortom/planets.html
          echo '</div>' >> majortom/planets.html

          # Main content with centered text in mosaic div
          echo '<main>' >> majortom/planets.html
          echo '<h1>Intergalactic Planets</h1>' >> majortom/planets.html
          echo '<div id="myMosaic">' >> majortom/planets.html

          # Correctly add images inside the myMosaic div
          for img in $(ls majortom/planets/pictures | grep -E '\.jpeg$|\.jpg$|\.gif$|\.svg$|\.png$|\.webp$'); do
            echo "<img src='https://brunurb.github.io/majortom/planets/pictures/$img' alt='Gallery Image' data-full='https://brunurb.github.io/majortom/planets/pictures/$img' class='thumbnail' onclick='showImage(this.dataset.full)' />" >> majortom/planets.html
          done

          echo '</div>' >> majortom/planets.html
          echo '<div id="overlay" onclick="closeOverlay(event)">' >> majortom/planets.html
          echo '  <div class="overlay-content">' >> majortom/planets.html
          echo '    <span id="close">×</span>' >> majortom/planets.html
          echo '    <img id="overlay-image" src="" alt="Full Size Image" />' >> majortom/planets.html
          echo '  </div>' >> majortom/planets.html
          echo '</div>' >> majortom/planets.html
          echo '</main>' >> majortom/planets.html

          # Footer with copyright symbol
          echo '<footer>© <a style="text-decoration:none" href="https://brunurb.github.io/">by brunurb</a></footer>' >> majortom/planets.html

          # Include jQuery and custom scripts
          echo '<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js"></script>' >> majortom/planets.html
          echo '<script type="text/javascript">' >> majortom/planets.html
          echo 'document.addEventListener("DOMContentLoaded", function() {' >> majortom/planets.html
          echo '    function showImage(src) {' >> majortom/planets.html
          echo '        const overlay = document.getElementById("overlay");' >> majortom/planets.html
          echo '        const overlayImage = document.getElementById("overlay-image");' >> majortom/planets.html
          echo '        overlayImage.src = src;' >> majortom/planets.html
          echo '        overlay.classList.add("show");' >> majortom/planets.html
          echo '    }' >> majortom/planets.html
          echo '    function closeOverlay(event) {' >> majortom/planets.html
          echo '        if (event.target.id === "overlay" || event.target.id === "close") {' >> majortom/planets.html
          echo '            const overlay = document.getElementById("overlay");' >> majortom/planets.html
          echo '            overlay.classList.remove("show");' >> majortom/planets.html
          echo '        }' >> majortom/planets.html
          echo '    }' >> majortom/planets.html
          echo '    const images = document.querySelectorAll("#myMosaic img");' >> majortom/planets.html
          echo '    images.forEach(image => {' >> majortom/planets.html
          echo '        image.addEventListener("click", function() {' >> majortom/planets.html
          echo '            showImage(this.dataset.full);' >> majortom/planets.html
          echo '        });' >> majortom/planets.html
          echo '    });' >> majortom/planets.html
          echo '    const overlay = document.getElementById("overlay");' >> majortom/planets.html
          echo '    overlay.addEventListener("click", closeOverlay);' >> majortom/planets.html
          echo '    const closeButton = document.getElementById("close");' >> majortom/planets.html
          echo '    closeButton.addEventListener("click", closeOverlay);' >> majortom/planets.html
          echo '});' >> majortom/planets.html
          echo '</script>' >> majortom/planets.html
          echo '</body></html>' >> majortom/planets.html

      - name: Commit changes
        run: |
          git config --local user.email "github-actions@github.com"
          git config --local user.name "GitHub Action"
          git add majortom/planets.html
          git commit -m "Regenerate majortom/planets.html for mosaic gallery with overlay" || echo "No changes to commit"
          git push origin master  # Make sure this matches your main branch
